# Supabase — from Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only, never expose to the client. Used for admin/service-role
# operations (e.g. in Route Handlers or Server Actions), not in proxy.ts.
SUPABASE_SERVICE_ROLE_KEY=
